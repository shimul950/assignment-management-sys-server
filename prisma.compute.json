{
  "region": "ap-southeast-1",
  "app": {
    "name": "assignment-submission-sys-server",
    "framework": "bun",
    "entry": "src/server.ts",
    "httpPort": 8000,
    "build": {
      "command": "pnpm run build",
      "outputDirectory": "."
    }
  }
}
